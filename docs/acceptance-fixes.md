# Правки по итогам разбора замечаний приёмки

Список конкретных правок, собранный при разборе замечаний из [acceptance-report.md](acceptance-report.md).
Обрабатывается пачкой после того, как закончим разбор всех замечаний.

Статус: 🔲 не сделано · ✅ сделано

---

## FIX-1 · Согласование числительных в текстах прогресса/инсайта ✅

**Откуда:** замечание №1 (US-4), уточнение при разборе.

**Файл:** [app/src/main/java/com/mooddiary/MainActivity.kt](../app/src/main/java/com/mooddiary/MainActivity.kt)

**Факты:**
- `recordWord(n)` ([:1509](../app/src/main/java/com/mooddiary/MainActivity.kt#L1509)) склоняет существительное для достижимых значений (1/2–3) корректно.
- Баг: в `ProgressBlock`, ветка `else` ([:1046](../app/src/main/java/com/mooddiary/MainActivity.kt#L1046)) — глагол «помогут» жёстко во множественном:
  `"Первый сигнал уже есть. Ещё $left ${recordWord(left)} помогут понять, устойчив ли он."`
  При `left == 1` («4 записи», статус `waiting`) выходит **«Ещё 1 запись помогут понять»** → нужно **«поможет»**.
- Дополнительно: `recordWord` наивен для 11–14 и 21/31… (в P0 недостижимо, но лучше сделать устойчиво).

**Что сделать:**
1. Починить согласование глагола в [:1046](../app/src/main/java/com/mooddiary/MainActivity.kt#L1046). Варианты:
   - переформулировать без зависимости от числа: например «Первый сигнал уже есть. Ещё немного записей — и станет ясно, устойчив ли он.» (тогда число не участвует в согласовании); **или**
   - подобрать глагол по числу: «поможет»/«помогут» через тот же приём, что «осталась»/«осталось» в [:1367](../app/src/main/java/com/mooddiary/MainActivity.kt#L1367).
2. (Опционально, задел) заменить `recordWord` на устойчивый помощник склонения по правилам русского языка (формы 1 / 2–4 / 5–20 с учётом 11–14 и десятков), чтобы любые будущие значения склонялись верно. Применить его во всех местах: [:1044](../app/src/main/java/com/mooddiary/MainActivity.kt#L1044), [:1046](../app/src/main/java/com/mooddiary/MainActivity.kt#L1046), [:1316](../app/src/main/java/com/mooddiary/MainActivity.kt#L1316), [:1367](../app/src/main/java/com/mooddiary/MainActivity.kt#L1367).

**Проверка:** прогнать состояния прогресса с остатком 1 и 2 (2 и 4 записи) — тексты грамматически корректны.

---

## FIX-2 · Узаконить стабильные ветки `found` с 3 записей (правка спеки) ✅

**Откуда:** замечание №2 (US-2), выбран вариант Б — привести спеку в соответствие с кодом.

**Решение:** код осознанно выдаёт `found / stable_low` и `found / stable_high` уже при 3–4 записях (это служит цели «первый инсайт после 3–5 записей»). Код НЕ меняем — правим документ, чтобы `stable_low`/`stable_high` были явно разрешены с 3 записей.

**Файл:** [docs/spec.md](spec.md)

**Что сделать:**
1. §5.2, [spec.md:181](spec.md#L181) — уточнить, что «явная связь» на 3–4 записях = **контраст (§5.3) ИЛИ подтверждённая стабильная ветка** `stable_low`/`stable_high`. Добавить, что стабильные ветки допустимы с 3 записей, уверенность при этом «низкая». Сейчас формулировка «только при явной связи» читается как «только контраст».
2. §5.3, блок «Ветки стабильного состояния внутри `found`» ([spec.md:205](spec.md#L205)) — добавить явную строку про порог: ветки считаются с минимума в 3 записи (как и весь `found`); примеры на 5 записях — иллюстративные, не порог. Уверенность на 3–4: «низкая».
3. Свериться, что US-2 крит. 2 ([spec.md:402](spec.md#L402)) остаётся согласованным (он ссылается на §5.3 — после правки §5.2 противоречие снимается).

**Проверка:** описание §5.2/§5.3 однозначно допускает `found / stable_*` при 3 записях; поведение кода `calculateInsight` ему соответствует.

---

## FIX-3 · Уточнить область оценки полезности в US-6 (правка спеки) ✅

**Откуда:** замечание №3 (US-6), выбран вариант А — привести спеку к намеренному поведению.

**Решение:** оценка полезности осознанно показывается только для доставленного инсайта (`found`), чтобы не замусоривать метрику «средняя оценка ≥4/5» заглушками и сообщением «связь не найдена». Код НЕ меняем — уточняем формулировку US-6.

**Файл:** [docs/spec.md](spec.md)

**Что сделать:**
1. US-6, крит. 1 ([spec.md:447](spec.md#L447)) — дописать, что оценка «полезно / не полезно» показывается **только для найденного инсайта (`found`)** и не показывается при статусах `not_enough_data`, `waiting`, `no_clear_pattern` (это прогресс/заглушки и «связь не найдена», а не доставленный инсайт).
2. По желанию — короткая ремарка рядом с метрикой успеха, что оценка ≥4/5 считается по `found`-инсайтам.

**Проверка:** US-6 явно ограничивает оценку статусом `found`; поведение `InsightScreen` ([MainActivity.kt:841](../app/src/main/java/com/mooddiary/MainActivity.kt#L841)) ему соответствует.

---

## FIX-4 · Обработка ошибки сохранения записи (правка кода) ✅

**Откуда:** замечание №4 (§6.3 / DoD-12), выбран вариант Б — минимальное упрочнение.

**Цель:** при неуспешном сохранении показать понятное сообщение и **оставить введённые значения** на экране check-in (не уходить на Home). Закрывает §6.3 и DoD-12.

**Файл:** [app/src/main/java/com/mooddiary/MainActivity.kt](../app/src/main/java/com/mooddiary/MainActivity.kt)

**Что сделать:**
1. `saveEntries` ([:330](../app/src/main/java/com/mooddiary/MainActivity.kt#L330)) → возвращать `Boolean`, писать через **`commit()`** вместо `apply()`:
   `return prefs.edit().putString(KEY_ENTRIES, json.toString()).commit()`
2. `save(draft)` ([:253](../app/src/main/java/com/mooddiary/MainActivity.kt#L253)) → возвращать `Boolean`, обернуть тело в `try/catch` (чтобы `requireNotNull`-путь [:263-267](../app/src/main/java/com/mooddiary/MainActivity.kt#L263-L267) не ронял приложение); вернуть результат `saveEntries(next)`, в `catch` — `false`.
3. `deleteEntry` ([:275](../app/src/main/java/com/mooddiary/MainActivity.kt#L275)) теперь тоже получает `Boolean` от `saveEntries` — возврат можно игнорировать (для удаления UI-сообщение не требуется).
4. Сигнатуру `onSave` у `CheckInScreen` сменить на `(DraftEntry) -> Boolean`.
5. Родитель — ветка `Screen.CheckIn` в `MoodDiaryApp` ([:421-429](../app/src/main/java/com/mooddiary/MainActivity.kt#L421-L429)):
   ```kotlin
   onSave = {
       val ok = store.save(it)
       if (ok) { refresh(); goHome() }
       ok
   },
   ```
6. В `CheckInScreen` ([:599](../app/src/main/java/com/mooddiary/MainActivity.kt#L599)) добавить `var saveError by remember { mutableStateOf(false) }`; в `onClick` кнопки «Сохранить» ([:717](../app/src/main/java/com/mooddiary/MainActivity.kt#L717)): сбросить `saveError = false`, вызвать `onSave(...)`, при `false` → `saveError = true`. При ошибке — не навигируем, локальные `remember`-значения (оценки, теги, шаг карусели) сохраняются автоматически.
7. Показать сообщение при `saveError` (Text в цвете ошибки или Snackbar), напр.: «Не удалось сохранить запись. Проверьте свободное место и попробуйте снова.»

**Проверка:** штатное сохранение работает как раньше (переход на Home); при принудительном сбое `commit()` (напр., подменить на `false` в отладке) — показывается сообщение, значения на экране остаются, повторное «Сохранить» срабатывает.

---

## FIX-5 · Примирить plan.md с фактической реализацией + подтверждение команды ✅

**Откуда:** замечание №5 (стек хранения), выбран вариант А — оставить `SharedPreferences` для P0, синхронизировать документы.

**Суть:** реализация P0 отходит от plan.md (v1.0). Код НЕ мигрируем. Правим plan.md, чтобы он отражал реальность, и выносим смену БД на явное подтверждение команды (конституция: «спросить перед сменой стека»).

**Отклонения кода от plan.md:**
| Заложено в plan.md | Фактически в коде | Комментарий |
|---|---|---|
| Room/SQLite ([plan.md:25](plan.md#L25), §3.1, §4) | `SharedPreferences` + JSON-блок (`LocalMoodStore`) | **существенное — требует ack команды** |
| Hilt DI ([:26](plan.md#L26)) | нет (единый файл, без ViewModel/Repository) | упрощение |
| Navigation Compose ([:28](plan.md#L28)) | ручной `when(screen)` | упрощение |
| Vico ([:29](plan.md#L29)) | Custom `Canvas` | **не отклонение** — это санкционированный fallback из таблицы рисков [plan.md:317](plan.md#L317) |
| Слоистая структура `di/data/domain/ui` ([:159](plan.md#L159)) | всё в `MainActivity.kt` | упрощение |

**Что сделать:**
1. Добавить в [plan.md](plan.md) короткий раздел «Отклонения реализации P0 (факт на 27.07.2026)» с таблицей выше — **не переписывая** исходный Room-дизайн (он остаётся целевым при росте P1). Явно указать: хранилище P0 = `SharedPreferences`+JSON; Room/Hilt/Navigation — отложены как рефакторинг под рост функциональности.
2. Обновить строки таблиц стека/зависимостей ([plan.md:25-30](plan.md#L25-L30), [:275-278](plan.md#L275-L278)) пометкой «P0: фактически не подключено / заменено на …».
3. **Вынести на команду** (Никита — код, Ярослав — спека): подтвердить смену БД Room→`SharedPreferences` для P0 как осознанное решение, зафиксировать в конституции/plan. Это ключевой пункт — не чисто редакторский.

**Проверка:** plan.md непротиворечиво описывает фактический стек P0; решение по БД зафиксировано с согласия команды.

---

## FIX-6 · Превью инсайта при `waiting` обещает то, чего нет (правка кода) ✅

**Откуда:** замечание пользователя при проверке (27.07.2026).

**Проблема:** на Home в карточке инсайта при статусе `waiting` показывается превью
> «Первый сигнал уже можно показать, но для уверенности нужно 5 записей. Открой, чтобы увидеть текущую картину.»
([MainActivity.kt:1453](../app/src/main/java/com/mooddiary/MainActivity.kt#L1453), функция `insightPreviewText`, ветка `InsightStatus.Waiting`).
Текст обещает, что сигнал **«уже можно показать»**, но при открытии экрана инсайта пользователь видит только сообщение «нужно ещё N записей до 5» — самого сигнала нет. Обещание не выполняется → подрывает доверие (а доверие для этого продукта критично).

**Файл:** [app/src/main/java/com/mooddiary/MainActivity.kt](../app/src/main/java/com/mooddiary/MainActivity.kt)

**Что сделать:**
1. Переписать превью ветки `InsightStatus.Waiting` в `insightPreviewText` ([:1453](../app/src/main/java/com/mooddiary/MainActivity.kt#L1453)) так, чтобы оно НЕ обещало готовый сигнал. Смысл: «первые записи есть, копим до 5 — открой посмотреть прогресс», без формулировки «уже можно показать». Финальную формулировку согласовать (тон §5.4: бережно, без давления).
2. Заодно проверить смежную фразу в `ProgressBlock` ([:1046](../app/src/main/java/com/mooddiary/MainActivity.kt#L1046)) «Первый сигнал уже есть…» — она мягче (не обещает показ), но стоит свериться, чтобы формулировки Home были согласованы и не создавали ложного ожидания.

**Проверка:** при статусе `waiting` (3–4 записи) превью на Home и содержимое экрана инсайта согласованы — превью не обещает того, чего нет при открытии.

**Сделано (27.07.2026):**
- `insightPreviewText` / `Waiting` → «Первые записи уже есть. Собираем до 5 для осторожного вывода — открой, чтобы увидеть прогресс.» (вариант A).
- `ProgressBlock` / ветка 3–4 → «Первый сигнал уже есть…» заменено на «Хорошее начало. Ещё $left … — и вывод будет надёжнее.» (та же ложная посылка «сигнал есть» устранена).
- Проверено на эмуляторе: оба текста на Home согласованы, ложного обещания нет.
